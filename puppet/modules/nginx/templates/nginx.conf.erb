server {
    listen 80;
    server_name localhost;

    location / {
        try_files $uri @weather_api;
    }

    location @weather_api {
        include uwsgi_params;
        uwsgi_pass unix:/run/uwsgi/weather_api.sock;
    }
}
